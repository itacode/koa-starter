services:
  app:
    build:
      context: .
    container_name: ${CONTAINER_NAME}
    restart: always
    ports:
      - '3000:3000'
    env_file:
      - .env

networks:
  default:
    name: backend
